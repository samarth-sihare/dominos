package com.bitwise.dominos.resource;

import com.bitwise.dominos.api.PizzaDS;
import com.bitwise.dominos.pizzadb.PizzaDB;
import com.bitwise.dominos.resource.pizza.PizzasRepository;
import org.junit.Assert;
import org.junit.Test;

/**
 * Created by kirankumarbs on 8/17/2016.
 */
public class PizzasRepositoryTest {

/*    @Test
    public void testPizzasResourceRepository(){
        //given
        PizzasRepository pRepository = new PizzasRepository();
        //when
        for(PizzaDS p : pRepository.getItems()){
            //then
            System.out.println(p.getPizzaName());
            Assert.assertEquals(p.getPizzaName(),PizzaDB.valueOf(p.getPizzaName()).pizza().getName());
        }
    }*/
}
